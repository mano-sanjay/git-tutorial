⚡ 10 Essential Git Commands
1️⃣ Initialize a Repository

Create a new Git repo in a folder.

git init

📌 Turns the current folder into a Git repository.

2️⃣ Clone a Repository

Download a repo from GitHub or another remote.

git clone <repository-url>

Example:

git clone https://github.com/user/project.git

📌 Copies the full repo to your machine.

3️⃣ Check Repository Status

Shows what files changed.

git status

You will see:

modified files

staged files

untracked files

💡 Developers run this constantly.

4️⃣ Add Files to Staging

Prepare files for commit.

git add filename

Add everything:

git add .

📌 Moves changes into the staging area.

5️⃣ Commit Changes

Save the changes permanently in Git history.

git commit -m "your message"

Example:

git commit -m "Fixed login bug"

💡 Always write clear commit messages.

6️⃣ View Commit History

See previous commits.

git log

Short version:

git log --oneline

📌 Shows commit hash, author, and message.

7️⃣ Push Code to Remote

Upload your commits to GitHub.

git push origin main

📌 Sends local commits to the remote repository.

8️⃣ Pull Latest Changes

Download updates from remote.

git pull origin main

📌 Combines:

git fetch + git merge
9️⃣ Create a Branch

Work on features safely.

git branch feature-login

Switch to it:

git checkout feature-login

Or faster:

git checkout -b feature-login
🔟 Merge a Branch

Combine changes.

git merge feature-login

📌 Usually done when a feature is finished.

🧠 Bonus Pro Commands (Hackers Love These)
Show differences
git diff
Undo staged file
git reset filename
Undo last commit (keep changes)
git reset --soft HEAD~1
Stash temporary changes
git stash
🏴‍☠️ Hacker Git Workflow

Most developers use this cycle:

git pull
git add .
git commit -m "message"
git push



⚡ 15 Advanced Git Commands Hackers / Power Users Use
1️⃣ git stash

Temporarily save uncommitted changes.

git stash

Restore them later:

git stash pop

💡 Useful when you need to switch branches quickly without committing.

2️⃣ git stash list

View all stashes.

git stash list
3️⃣ git stash apply

Apply a stash without removing it.

git stash apply stash@{0}
4️⃣ git cherry-pick

Apply a commit from another branch.

git cherry-pick <commit-hash>

📌 Great for copying one fix without merging the whole branch.

5️⃣ git rebase

Rewrite commit history cleanly.

git rebase main

💡 Keeps history linear and clean.

6️⃣ Interactive Rebase

Edit commits before merging.

git rebase -i HEAD~5

You can:

squash commits

edit messages

remove commits

7️⃣ git reset

Undo commits.

Soft reset (keep changes):

git reset --soft HEAD~1

Hard reset (delete changes):

git reset --hard HEAD~1

⚠️ Hard reset destroys changes.

8️⃣ git reflog

See everything Git remembers, even deleted commits.

git reflog

💡 This command can recover lost commits.

9️⃣ git blame

See who modified each line in a file.

git blame filename

Useful for debugging.

🔟 git log --graph

Visualize commit history.

git log --oneline --graph --all

Example output:

* commit1
|\
| * commit2
* | commit3
1️⃣1️⃣ git clean

Remove untracked files.

Preview first:

git clean -n

Delete them:

git clean -f
1️⃣2️⃣ git bisect

Find the commit that introduced a bug.

Start binary search:

git bisect start

Mark bad commit:

git bisect bad

Mark good commit:

git bisect good <commit>

Git automatically finds the bug.

1️⃣3️⃣ git show

Inspect a specific commit.

git show <commit-hash>

Shows:

changes

author

commit message

1️⃣4️⃣ git tag

Create version tags.

git tag v1.0

Push tags:

git push origin --tags

Used for releases.

1️⃣5️⃣ git remote

Manage remotes.

Show remotes:

git remote -v

Add remote:

git remote add origin <repo-url>
🧠 Pro Developer Aliases (Huge Time Saver)

Add shortcuts:

git config --global alias.st status
git config --global alias.co checkout
git config --global alias.cm commit
git config --global alias.br branch

Then you can run:

git st
git co main
git cm -m "update"

⚡ Much faster.

🏴‍☠️ Ultimate Git Power Command

Many developers love this one:

git log --oneline --graph --decorate --all

It shows a beautiful commit tree.